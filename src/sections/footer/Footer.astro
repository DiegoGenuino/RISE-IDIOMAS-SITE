---
import NewsletterForm from '../../components/newsletter/NewsletterForm.astro';
import { site, socialLinks, whatsappLink } from '../../data/site';

/**
 * Rodapé.
 *
 * Referência: o padrão de wordmark gigante ancorado no fim da página — o gesto
 * que Vercel, Linear e Framer usam para fechar o scroll com a marca em vez de
 * uma linha de copyright. A versão anterior era quatro colunas de links e mais
 * nada, e o site acabava sem assinatura.
 *
 * O `master` já tinha essa intenção num marquee de "RISE" em contorno; aqui ela
 * volta como uma palavra só, à largura do container, que se preenche ao hover.
 */
const currentYear = new Date().getFullYear();

const columns = [
  {
    title: 'Programas',
    links: [
      { label: 'Executivo', href: '/#modalidades' },
      { label: 'Acadêmico', href: '/#modalidades' },
      { label: 'Jurídico', href: '/#modalidades' },
      { label: 'Preparatórios', href: '/#modalidades' },
      { label: 'Teens & Kids', href: '/#modalidades' },
    ],
  },
  {
    title: 'Explorar',
    links: [
      { label: 'Metodologia', href: '/#metodologia' },
      { label: 'Trilha A1–C2', href: '/#trilha' },
      { label: 'Teste de nível', href: '/#nivel' },
      { label: 'Planos', href: '/#planos' },
      { label: 'Blog', href: '/blog' },
    ],
  },
  {
    title: 'Contato',
    links: [
      { label: 'WhatsApp', href: whatsappLink('Olá, vim pelo site da Rise Idiomas.') },
      { label: site.email, href: `mailto:${site.email}` },
      { label: 'Como chegar', href: site.mapsUrl },
    ],
  },
];

const socialPaths: Record<string, string> = {
  Instagram:
    'M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z',
  LinkedIn:
    'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z',
  Facebook:
    'M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z',
  YouTube:
    'M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z',
};
---

<footer class="ds-dark bg-abyss-deep relative overflow-hidden text-white">
  <div class="ds-grain-layer" aria-hidden="true"></div>

  <!-- ── Newsletter ────────────────────────────────────────────────────── -->
  <div class="border-abyss-line/40 relative border-b">
    <div class="ds-shell ds-grid items-center py-14">
      <div class="col-span-full lg:col-span-6">
        <p class="text-title max-w-sm font-semibold text-white">
          Uma dose de idioma na sua caixa de entrada
        </p>
        <p class="text-abyss-ink mt-3 max-w-sm text-sm leading-relaxed">
          Vocabulário, cultura e método — o que a equipe escreve entre uma turma e outra. Sem spam.
        </p>
      </div>

      <div class="col-span-full flex lg:col-span-6 lg:justify-end">
        <NewsletterForm />
      </div>
    </div>
  </div>

  <!-- ── Links ─────────────────────────────────────────────────────────── -->
  <div class="ds-shell ds-grid relative items-start py-14">
    <div class="col-span-full flex flex-col gap-6 lg:col-span-3">
      <p class="text-abyss-ink max-w-xs text-sm leading-relaxed">
        Transformamos a maneira como você aprende uma segunda língua — uma jornada mais leve e
        eficaz, com foco em você e no seu crescimento.
      </p>

      <ul class="flex gap-2">
        {
          socialLinks.map((social) => (
            <li>
              <a
                href={social.url}
                target="_blank"
                rel="noopener noreferrer"
                aria-label={social.label}
                class="ds-social"
              >
                <svg
                  viewBox="0 0 24 24"
                  width="16"
                  height="16"
                  fill="currentColor"
                  aria-hidden="true"
                >
                  <path d={socialPaths[social.label]} />
                </svg>
              </a>
            </li>
          ))
        }
      </ul>
    </div>

    {
      columns.map((column) => (
        <nav aria-label={column.title} class="col-span-2 flex flex-col gap-4 lg:col-span-3">
          <h2 class="ds-mono text-abyss-ink/50">{column.title}</h2>
          <ul class="flex flex-col gap-2.5">
            {column.links.map((link) => {
              const external = link.href.startsWith('http') || link.href.startsWith('mailto:');
              return (
                <li>
                  <a
                    href={link.href}
                    target={external ? '_blank' : undefined}
                    rel={external ? 'noopener noreferrer' : undefined}
                    class="ds-foot-link"
                  >
                    {link.label}
                  </a>
                </li>
              );
            })}
          </ul>
        </nav>
      ))
    }
  </div>

  <!-- ── Assinatura ─────────────────────────────────────────────────────
       Puro contorno; o preenchimento acende de baixo para cima ao hover.
       `aria-hidden` porque o nome da marca já está no link do topo — aqui
       é ornamento, não conteúdo.                                        -->
  <div class="ds-wordmark-wrap">
    <p class="ds-wordmark" aria-hidden="true">RISE</p>
  </div>

  <!-- ── Legal ─────────────────────────────────────────────────────────── -->
  <div class="border-abyss-line/40 relative border-t">
    <div
      class="ds-shell flex flex-col items-center justify-between gap-3 py-6 text-center sm:flex-row sm:text-left"
    >
      <p class="ds-mono text-abyss-ink/50">© {currentYear} Rise Idiomas · {site.address}</p>

      <div class="flex items-center gap-5">
        <span class="ds-mono text-abyss-ink/50 flex items-center gap-2">
          <span class="ds-clock-dot" aria-hidden="true"></span>
          São Paulo <span id="rise-clock" class="text-white">--:--</span>
        </span>
        <span class="ds-mono text-abyss-ink/50" title="Idiomas ensinados na Rise">
          {site.languages.map((language) => language.slice(0, 3).toUpperCase()).join(' / ')}
        </span>
      </div>
    </div>
  </div>
</footer>

<script is:inline>
  (function () {
    const clock = document.getElementById('rise-clock');
    if (!clock) return;

    function tick() {
      clock.textContent = new Date().toLocaleTimeString('pt-BR', {
        timeZone: 'America/Sao_Paulo',
        hour: '2-digit',
        minute: '2-digit',
      });
    }

    tick();
    setInterval(tick, 30000);
  })();
</script>

<style>
  .ds-grid {
    row-gap: 2.5rem;
  }

  /* ── Social ─────────────────────────────────────────────────────────── */
  .ds-social {
    display: grid;
    place-items: center;
    width: 2.25rem;
    height: 2.25rem;
    color: var(--color-abyss-ink);
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-abyss-line) 80%, transparent);
    transition:
      color var(--dur-fast) var(--ease),
      background-color var(--dur-fast) var(--ease),
      transform var(--dur-base) var(--ease-card);
  }

  .ds-social:hover {
    color: var(--color-abyss-deep);
    background-color: #fff;
    transform: translate3d(0, -2px, 0);
  }

  /* ── Links ──────────────────────────────────────────────────────────── */
  .ds-foot-link {
    position: relative;
    display: inline-block;
    font-size: 0.875rem;
    color: var(--color-abyss-ink);
    transition: color var(--dur-fast) var(--ease);
  }

  /* Sublinhado que cresce da esquerda — só transform. */
  .ds-foot-link::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 100%;
    height: 1px;
    background-color: currentColor;
    transform: scaleX(0);
    transform-origin: left center;
    transition: transform var(--dur-base) var(--ease-card);
  }

  .ds-foot-link:hover {
    color: #fff;
  }

  .ds-foot-link:hover::after {
    transform: scaleX(1);
  }

  /* ── Wordmark ───────────────────────────────────────────────────────── */
  .ds-wordmark-wrap {
    position: relative;
    padding-inline: clamp(1rem, 4vw, 3rem);
    overflow: hidden;
  }

  .ds-wordmark {
    margin: 0;
    /* Acompanha a largura da janela em vez de ter um tamanho fixo. */
    font-size: clamp(5rem, 27vw, 22rem);
    line-height: 0.85;
    font-weight: 700;
    letter-spacing: -0.06em;
    text-align: center;
    user-select: none;

    color: transparent;
    -webkit-text-stroke: 1.25px color-mix(in srgb, var(--color-brand-400) 55%, transparent);
    /* O preenchimento é uma faixa de altura zero ancorada em baixo; no hover
       ela sobe até cobrir a palavra. Anima background-size, que não mexe em
       layout nenhum. */
    background-image: linear-gradient(180deg, var(--color-brand-400), var(--color-aqua));
    -webkit-background-clip: text;
    background-clip: text;
    background-size: 100% 0%;
    background-repeat: no-repeat;
    background-position: 50% 100%;
    transition:
      background-size var(--dur-scene) var(--ease-card),
      -webkit-text-stroke-color var(--dur-base) var(--ease);
  }

  @media (hover: hover) {
    .ds-wordmark-wrap:hover .ds-wordmark {
      background-size: 100% 100%;
      -webkit-text-stroke-color: transparent;
    }
  }

  /* ── Relógio ────────────────────────────────────────────────────────── */
  .ds-clock-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background-color: var(--color-lime);
    box-shadow: 0 0 0 0 rgba(180, 244, 97, 0.6);
    animation: ds-clock-pulse 2.6s ease-out infinite;
  }

  @keyframes ds-clock-pulse {
    70% {
      box-shadow: 0 0 0 6px rgba(180, 244, 97, 0);
    }
    100% {
      box-shadow: 0 0 0 0 rgba(180, 244, 97, 0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ds-clock-dot {
      animation: none;
    }
  }
</style>
